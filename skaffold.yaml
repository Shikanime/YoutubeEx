apiVersion: skaffold/v1beta10
kind: Config
build:
  artifacts:
  - image: youtube-ex/web
    context: apps/web
  local:
    useBuildkit: true
deploy:
  helm:
    releases:
    - name: youtube-ex-web
      chartPath: deployments/web
      values:
        image: youtube-ex/web
