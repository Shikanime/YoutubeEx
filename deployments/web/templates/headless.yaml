apiVersion: v1
kind: Service
metadata:
  name: {{ include "youtube-ex-web.fullname" . }}-headless
  labels:
    app: {{ template "youtube-ex-web.name" . }}
    chart: {{ template "youtube-ex-web.chart" . }}
    release: {{ .Release.Name | quote }}
    heritage: {{ .Release.Service | quote }}
spec:
  clusterIP: None
  selector:
    app: {{ template "youtube-ex-web.name" . }}
    release: {{ .Release.Name | quote }}
